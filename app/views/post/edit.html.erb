<main>
  <h1>投稿編集画面</h1>
  <div class="post-total">
    <%= form_tag("/post/#{@post.id}/update", {multipart: :true}) do %>
      <textarea name="content"><%= @post.content %></textarea>
      <input type="submit" value="保存" ></input>
    <% end %>
  </div>
</main>